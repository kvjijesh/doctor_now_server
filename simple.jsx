{/* <div className="modal-buttons">
              {selectedDoctor?.is_blocked ? (
                <Button
                  variant="contained"
                  color="secondary"
                  onClick={() =>
                    handleBlockButtonClick(selectedDoctor?._id, false)
                  }
                >
                  Unblock
                </Button>
              ) : (
                <Button
                  variant="contained"
                  color="primary"
                  onClick={handleApproveButtonClick}
                  disabled={selectedDoctor?.isVerified}
                >
                  Approve
                </Button>
              )}
              <Button
                variant="contained"
                color="secondary"
                onClick={() =>
                  handleBlockButtonClick(
                    selectedDoctor?._id,
                    !selectedDoctor?.is_blocked
                  )
                }
                className="block-button"
              >
                {selectedDoctor?.is_blocked ? "Unblock" : "Block"}
              </Button>
            </div> */}


            // const mypromise= new Promise((resolve,reject)=>{

            // })